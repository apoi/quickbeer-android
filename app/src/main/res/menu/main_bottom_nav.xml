<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/top_beers"
        android:icon="@drawable/ic_list"
        android:contentDescription="@string/top_beer_description"
        android:title="@string/top_beers_title" />
    <item
        android:id="@+id/style_list"
        android:icon="@drawable/ic_list"
        android:contentDescription="@string/style_list_description"
        android:title="@string/style_list_title" />
    <item
        android:id="@+id/about"
        android:icon="@drawable/ic_feedback"
        android:contentDescription="@string/about_description"
        android:title="@string/about_title" />
</menu>
